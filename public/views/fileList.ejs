<!DOCTYPE html>
<html>
<meta charset='UTF-8' />
<meta name="viewport" id="vp" content="width=device-width,initial-scale=1, maximum-scale=1, user-scalable=no">
<head>
    <title><%= title %></title>
    <link href="/css/style.css" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="https://cdn0.iconfinder.com/data/icons/octicons/1024/markdown-128.png" />
</head>

<body class="bd">
    <div class="container list-box">
        <h3><%= title %></h3>
        <hr>
        <ul>
            <% list.forEach(function(item){ %>
                <li class="file <%= item.isDirectory ? "file-dir" : item.isMd ? "file-md" : "file-file"%>">
                    <a href='<%= item.url %>'><%= item.name %></a>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>